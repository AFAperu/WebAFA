{
  "name": "header-footer-components",
  "version": "1.0.0",
  "description": "Static website with reusable header and footer components",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.4",
    "jsdom": "^23.0.1",
    "fast-check": "^3.15.0"
  }
}
